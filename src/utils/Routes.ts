enum Screens {
  ONBOARDING = "OnboardingScreen",
  LOGIN = "LoginScreen",
  SIGNUP = "SignupScreen",
  HOME = "Home",
  SEARCH = "Search",
  LIBRARY = "Library",
  AUTHOR = "Author",
  BOOK = "Book",
  CATEGORY = "Category",
}

enum Stacks {
  UNSIGNED = "UnsignedStack",
  SIGNED = "SignedStack",
}

enum Flows {
  MAIN = "MainFlow",
  SIDE = "SideFlow",
}

export { Screens, Stacks, Flows };
