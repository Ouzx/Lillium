enum Screens {
  ONBOARDING = "OnboardingScreen",
  LOGIN = "LoginScreen",
  SIGNUP = "SignupScreen",
  HOME = "HomeScreen",
}

enum Stacks {
  UNSIGNED = "UnsignedStack",
  SIGNED = "SignedStack",
}

export { Screens, Stacks };
