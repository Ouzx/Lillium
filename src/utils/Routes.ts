enum Screens {
  ONBOARDING = "OnboardingScreen",
  LOGIN = "LoginScreen",
  SIGNUP = "SignupScreen",
  HOME = "Home",
  SEARCH = "Search",
  LIBRARY = "Library",
}

enum Stacks {
  UNSIGNED = "UnsignedStack",
  SIGNED = "SignedStack",
}

export { Screens, Stacks };
